import otherimport from './exampleMJS.mjs'

const dateWrapper = Date
const setTimeoutWrapper = setTimeout
const fetchWrapper = fetch

export {
  dateWrapper,
  setTimeoutWrapper,
  fetchWrapper,
  otherimport
}
